
= h @campaign.compiled_template_for(@campaign.subscribers.first).html_safe

-#%img{src: open_campaign_track_url(@campaign, @campaign.subscribers.first.encoded_id,  format: :gif), width:"1", height: "1" }
